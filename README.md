# ft_onion
A minimal Docker-based setup that runs a Tor onion service fronted by Nginx with a hardened SSH server. It’s meant for learning how to host services over Tor, configure Nginx as a reverse proxy, and secure SSH access using keys instead of passwords.
